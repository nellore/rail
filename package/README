Scripts that package things up.

package_rail.sh: Put together archive with all files needed for someone else to run Rail
package_igenomes.sh: In an iGenomes directory, put together an archive that can be used with Rail
